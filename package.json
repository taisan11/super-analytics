{
  "name": ".",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "$npm_execpath run build && wrangler pages deploy"
  },
  "dependencies": {
    "@std/http": "npm:@jsr/std__http",
    "hono": "^4.4.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240614.0",
    "@hono/vite-cloudflare-pages": "^0.4.1",
    "@hono/vite-dev-server": "^0.12.2",
    "esbuild": "^0.21.5",
    "vite": "^5.3.1",
    "wrangler": "^3.60.3"
  }
}